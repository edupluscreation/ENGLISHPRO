[build]
  command = "npm run build"
  publish = "dist"
  ignore = "git log -1 --pretty=%B | grep -iE '\\[skip netlify\\]|\\[skip ci\\]' && exit 0 || exit 1"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
